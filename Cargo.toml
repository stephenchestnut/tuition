[package]
name = "tuition"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "A tiny command-driven terminal slide presenter"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
crossterm = "0.28"
